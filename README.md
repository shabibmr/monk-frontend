# frontend-code

Isolated Flutter workspace for Influencers Monk web.

```
frontend-code/
  apps/web/           # Flutter web app
  packages/api_client # Interim Dio client (replace via OpenAPI gen)
  packages/shared     # Enums / error codes
```

See `apps/web/README.md` for run instructions.
